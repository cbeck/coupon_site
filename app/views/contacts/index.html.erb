<% @title = "Contact Messages" %>

<%= render :partial => "shared/admin_menu" %>
        <div>
          <div id="page_menu" class="tabs">
            <%= render :partial => "tabs"%>
          </div>

          <div id="contact_list">
            <%= render :partial => "contacts", :object => @contacts %>
          </div>
        </div>
        <div class="clear_fix">&nbsp;</div>
